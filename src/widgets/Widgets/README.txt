This folder contains working (as in being worked on) versions of the widgets for the Book of Mormon Voices site. They should all work, but may not be final versions

In an attempt to stylistically unify the widgets, they (should) all inherit css color variables from one unified stylesheet (colors.css).
The data on these widgets is not guaranteed to be correct, and they need updating in how they fetch data.
 


